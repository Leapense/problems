#include <bits/stdc++.h>
using namespace std;

class Queuestack
{
private:
    int n;
    vector<int> t;
    vector<long long> b;

public:
    Queuestack(int n, const vector<int> &t, const vector<long long> &b)
    {
        this->n = n;
    }
}